# Marks the defect_detector test suite as a package so pytest imports its
# modules under a unique dotted name (avoids the duplicate test_benchmark.py
# basename clash with other services when collecting from the repo root).
