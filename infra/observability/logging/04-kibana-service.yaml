---
apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: railos
  labels:
    app: kibana
    app.kubernetes.io/part-of: railos-pilot
spec:
  type: ClusterIP
  selector:
    app: kibana
  ports:
    - name: http
      port: 5601
      targetPort: http
